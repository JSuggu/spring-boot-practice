server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: multiple-instances
          uri: https://github.com/JSuggu/spring-boot-practice
          search-paths: config-data
  application:
    name: config-service
