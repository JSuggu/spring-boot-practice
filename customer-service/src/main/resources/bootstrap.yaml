spring:
  application:
    name: customer-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
    compatibility-verifier:
      enabled: false
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:100/realms/tutorial-realm
