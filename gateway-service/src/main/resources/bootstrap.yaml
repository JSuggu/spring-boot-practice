spring:
  application:
    name: gateway-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:100/realms/tutorial-realm
